

const printers = {
    errorPrint: (message) => {
        console.log(message);
    },

    successPrint: (message) => {
        console.log(message);
    },

    requestPrint: (message) => {
        console.log(message);
    }
}
//catch error?

module.exports = printers;
